// Sum Numbers
// function sum(numbers) {
//     "use strict";
//     return numbers.reduce((acc, val) => acc + val, 0);

// };

const sum = (num) => num.reduce((acc,val) => acc+val, 0); 
console.log(sum([-2.398]));